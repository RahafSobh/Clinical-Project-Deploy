# Frontend (Vite) - set on the Render Static Site
VITE_API_BASE_URL=https://YOUR-BACKEND-SERVICE.onrender.com

# Backend - set on the Render Web Service
OPENROUTER_API_KEY=replace_me
OPENROUTER_MODEL=openrouter/auto
# After the frontend is live, replace * with its URL if desired.
ALLOWED_ORIGINS=*
